server:
  port: 8082
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pbms-userregister
    username: postgres
    password: admin
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
  instance:
    hostname: localhost