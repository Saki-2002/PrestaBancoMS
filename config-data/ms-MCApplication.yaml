server:
  port: 8083
spring:
  datasource:
    uri: jdbc:postgresql://localhost:5432/pbms-mcapplication
    username: postgres
    password: admin
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
  instance:
    hostname: localhost